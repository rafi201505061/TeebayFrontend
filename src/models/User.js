export class User {
  constructor({ id, userId, firstName, address, lastName, email, phoneNo }) {
    this.id = id;
    this.userId = userId;
    this.firstName = firstName;
    this.address = address;
    this.lastName = lastName;
    this.email = email;
    this.phoneNo = phoneNo;
  }
}
